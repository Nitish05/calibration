# PlatformIO build artifacts — regenerated by `pio run`
.pio/build/
.pio/libdeps/megaatmega2560/

# IDE / machine-specific paths
.vscode/
.claude/

# Mirrored copy of the host-side serial-protocol doc — keep the
# canonical one at the repo root, not duplicated here
SERIAL_COMMANDS.md
